# Insurance network metrics R2-04

`data/home/insurance-network-metrics-v1.json` remains the specialist authority. Schema insurance-network-metrics-v1; contractRevision ATH-METRICS-R2-04. The new read-only `/api/network-metrics` endpoint serves the same contract.

Run `npm run home:metrics`, then `npm run check:metrics`. Generation reads committed accepted inputs, never the old output (except its generatedAt for read-only --check). Validation exits nonzero on source/output drift, invalid counts, identity changes requiring review, unsafe profile attachments, null-to-zero substitution, NAIC match reconciliation or Illinois name-field partition failure. CI runs the focused checks before existing full tests. Source fingerprints exclude generation clocks and normalize LF/CRLF.

National input: data/home/insurance-metric-census-r2-04.json is a read-only exact-count Production capture at 2026-09-13T15:23:00.840Z. Count-query retrieval is not official source freshness. Agencies 82,071 and legal insurers 6,185 match the previously accepted identity baselines. `node --import tsx scripts/export_network_metric_census.mjs --out=<review-candidate.json>` can acquire a future candidate using existing credentials; it performs only reads, fails on absent exact-count headers and does not publish the candidate. Review actual identity changes before replacing a census input. The sequential queries are not an atomic database snapshot; independent live comparison passed for this release.

State inputs remain canonical accepted lib/*-intelligence/accepted-snapshot.json artifacts. The adapter validates and exports `reconciliation.states`, `reconciliation.measures`, `reconciliation.identity`, `acceptedStateSnapshots` and LF-normalized acceptedInputHashes. Four-state evidence measures include value/null, explicit grain, source artifact/field, sourceAsOf, snapshotAsOf, retrievedAt and generatedAt. Dated report clocks additionally preserve sourcePeriodEnd and reportedAsOf. Full accepted context preserves other research-only capability limits; numeric fields must never be blindly summed.

`homepageInputs` derives agency multi-state buckets from the accepted INS-HOME-003B SQL lock and LOA dataset counts from INS-NAT-012. Runtime homepage default/projection inputs consume generated counts. Published state page counts describe pages, not complete regulatory coverage. A new source shape needs a reviewed grain/capability adapter before regeneration, but no homepage number edits.

Illinois: 2,896 Director's Order observations / IDs; company-name 782, person-name 2,321, both 210, union 2,893, neither 3. Names are not exact identity. All accepted company/producer associations, profile attachments, new organizations, enrichments and graph writes remain zero. Five search/research capabilities remain null, never zero. Director's Order is not insurer, agency, complaint or violation.

Colorado complaint matching is 291 distinct exact NAIC identities out of 292 source NAIC IDs, across 415 company-by-line observations. New York groups are 300 distinct group identifiers; 128 auto-market source observations have 127 exact existing identities. Neither measurement creates public profiles. Virginia report, regulatory action, market-conduct and financial-exam layers remain separate.

Prompt 5: consume this revision by metric key and explicit grain. The legacy licensed_insurance_companies key now says Legal insurer identities; it never means a current 50-state authorization census. The NY directory grain is ny_dfs_company_directory_row. Illinois search-only valueState is UNKNOWN and value null. Accepted NAIC matches do not grant profile-attachment permission. Update Ask's prior pinned fingerprint deliberately; no Ask files were changed.
