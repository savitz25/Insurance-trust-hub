# R1-013 diagnosis and plan

Model: GPT-6 Astra / High - USER-CONFIRMED; no independent session metadata. Remote verified. Starting main 8051be7d5169929e42155cab81effa94178e8aad, canonical deployment dpl_CPcFskYZgjdf8YfXJzupT19iQxFG. Fresh th-search-r1-013 worktree. No applicable AGENTS.md or visible overlapping ticket assignment/open PR. Other dirty/old Insurance worktrees left untouched.

The 18-case matrix executes the unchanged local server boundary and canonical API. Strong exact-ID behavior coexists with name-to-cohort substitution: class detection misses source abbreviation INS CORP; name recognition excludes any recognized state. Brand fallback assumes agency. Name results lack source-field match evidence and revalidated task continuation. NPN evidence detection matches appoint but not appointed, and extracts only for, not with. Native page appends English filters and slices 180 characters; API independently slices 180 while parser slices 400. Public directory handoff loses product/requested-city context and offers fixed sample queries instead of ZIP entry. Address/NOT_ACQUIRED limitations lose typed conditions; domicile limitation retains KNOWN. Database errors are discarded by several lookup/list functions, allowing source failure to resemble an empty result.

Repair the existing interpreter/executor with additive typed intent, requested conditions, refinement/recovery, terminal states and source-name evidence. Keep insurance-ask-v1 and publication boundaries. Validate the full 180-character public bound once and apply typed overrides under one boundary. Exact identifiers remain first. Use bounded source legal/display-name matching, class-safe candidates and server-revalidated selection. Preserve local place/product context in a ZIP form and an explicit directory handoff; never promote directory rows to regulatory identities. Preserve source-native LOA/appointment and honest incomplete evidence. Add relevant maintained official/state actions, source-error propagation and compatible structured adapter handling. Test real planning/retrieval/projection/rendering with fixture adapters, then mutate, regress, browser-test, review and normally release.

No data, schema, source ingestion or publication changes. Existing working state/identifier/cohort cases remain protections, not claimed defects.

Additional observed defects during actual browser/review: source errors were previously hidden by empty arrays; the source-constrained LOA operation must restrict its issuer dataset, not just another credential on the entity. Appointer lookup must search the source carrier class. Invalid nested footer anchors cause hydration replacement and erase the first typed query; a minimal wrapper correction is required for the ticket's first-Enter acceptance. Full details and bounded source evidence are in source-capability-matrix.md and review.md.

Baseline limitations: global ESLint 15 errors/55 warnings; INS-DIR-BAIL and insurer 001?006 stale homepage fingerprints (001/002 also stale homepage Trust Score text assertions); phase6b appointment-label assertion. All reproduced on exact baseline. No expectations in those unrelated gates were weakened. The earlier TH-SEARCH-001E raw.length<=400 expectation was updated because R1-013 explicitly requires preserving/rejecting overlong input rather than slicing it. The old name-refusal expectation was updated to real source-backed candidate retrieval. A stale homepage CTA-string assertion now checks the actual shared Ask form boundary.
